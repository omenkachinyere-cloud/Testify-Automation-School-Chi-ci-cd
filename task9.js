function calculateAreaOfRectangle(length, width) {
    const area = length * width;
    console.log("The area of the rectangle with length " + length + " and width " + width + " is: " + area);
}
calculateAreaOfRectangle(3.2, 9.7);